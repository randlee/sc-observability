# C.2 immutable sc-lint source adoption evidence

The child adopts the reviewed source-installer candidate from
`sc-publish` PR99 (`be1705401d58008f862dd0829996fc50667e4ab1`) and configures
the caller-owned manifest with:

```toml
[project]
sc_lint_source_revision = "ba2d9bf622c1604e3f017c728040906b90e71bce"
```

The candidate was checked out detached and installed into an isolated scratch
consumer with the generated source-installer helper:

```sh
python3 .github/scripts/setup_sc_lint_source.py resolve
# SC_LINT_SOURCE_REVISION=ba2d9bf622c1604e3f017c728040906b90e71bce
python3 .github/scripts/setup_sc_lint_source.py install
python3 .github/scripts/setup_sc_lint_source.py smoke
```

The source receipt records matching source and package versions (`0.6.0`),
source revision `ba2d9bf622c1604e3f017c728040906b90e71bce`, and a consumer-local
Python wheel at `.sc-lint/venv/bin/python3`. Binary and wheel SHA-256 receipts
were generated by the installer; no released sc-lint artifact or fallback
wheel was used.

The captured receipt included wheel SHA-256
`5ffd6d2035c81b0d0c0119b1ddff5edd2f7fd2bc4b568b8fc3dbc8dbbabeffb4` and
the source-built `sc-lint` binary SHA-256
`2a55358cad60b5341807b682e090e1f6911cb6213fe710422563697408267bf3`; the
receipt also recorded the sibling backend hashes and exact source checkout.

The source smoke returned a successful command envelope but a failing lint
status, which is retained rather than suppressed:

```text
ok=true
data.status=fail
data.tool=sc-lint-boundary
data.version=0.6.0
data.scanned_crates=9
data.findings=41
Root discovery/backend execution succeeded; reported lint status=fail
```

The 41 findings are visible consumer diagnostics (cycles and manifest-policy
reports) and remain under separate triage. `ok=true` means the CLI/backend
executed successfully; it is not a claim that the consumer is lint-clean.
The previous `unsupported impl owner type &FieldValue<'_, T>` parser crash is
absent, proving the source parser adoption removed the crash without changing
consumer Rust or bypassing lint.

Python/source compatibility was verified by the installer receipt and focused
source-helper tests: **92 passed, 9 skipped** across
`test_setup_sc_lint_source.py`, `test_install.py`, and
`test_release_artifacts.py`.

Windows uses the same supported path in
`.github/actions/setup-sc-lint/action.yml`: `runner.os == 'Windows'` invokes
`python .github/scripts/setup_sc_lint_source.py resolve`, then `install`, then
`smoke`, with `SC_LINT_SOURCE_REVISION_INPUT` and `SC_LINT_REPOSITORY` passed
through the composite action. Actual Windows runner execution is pending and
is not claimed by this local macOS evidence; the smallest follow-up is a
Windows-2022 release-preflight consumer job executing that existing action path
and archiving `.sc-lint/source-install.json` plus the smoke JSON.

PR187 adds that bounded consumer-owned qualification workflow at
`.github/workflows/sc-lint-source-preflight.yml`. It runs the existing
composite action on `ubuntu-latest` and `windows-2022` with `contents: read`, a
35-minute job timeout, exact-revision receipt/version assertions, and
artifact retention for the receipt and full smoke output. The local harness
ran the same receipt verifier against the source-built consumer and reported
`ok=true`, `status=fail`, `finding_count=41`, version `0.6.0`, and matching
binary/wheel digests. Actual Windows CI results remain pending.

No credentials, tags, merges, or publication were used. PHC-QA-019 source
adoption is implemented here; the 41 visible diagnostics and final preflight
closure remain open.
