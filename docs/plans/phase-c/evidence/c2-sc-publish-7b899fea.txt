# C.2 sc-publish correction evidence

Reviewed upstream revision: `7b899fea2325b6bda55a5d061f2c507366246974`.

The installer was run from the exact detached upstream checkout:

```sh
python3 "$SC_PUBLISH_CHECKOUT/plugins/sc-publish/install.py" --input install.json .
python3 "$SC_PUBLISH_CHECKOUT/plugins/sc-publish/install.py" --dry-run --input install.json .
```

The repeat dry-run returned `Publish-kit assets are in sync.` (exit 0). The
upstream revision contains the bounded subprocess test fixes and three real
stalled-child regressions; no local edits to installer-managed files remain.
The upstream CI evidence is run `35413991243` (source 169 passed/10 skipped;
consumer 168 passed/11 skipped), as reported in the PR184 handoff. Local
consumer-owned corrections remain separate from installer-managed assets.
