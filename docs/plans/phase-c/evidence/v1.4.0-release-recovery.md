# v1.4.0 release recovery

The authorized production release on main `2e27142afebf1d138d50d1468dbef182a187147e` passed final preflight [35482394043](https://github.com/randlee/sc-observability/actions/runs/35482394043), then partially failed in [Release 35482638938](https://github.com/randlee/sc-observability/actions/runs/35482638938).

## Findings

- Linux x86_64 and macOS arm64 wheel builds: `rustup target add` selected the repository toolchain file and attempted component installation, failing with `detected conflict: bin/cargo-fmt`. Select the manifest-installed toolchain explicitly for release subprocesses while retaining the repository development toolchain policy.
- Windows wheel verification: the correct `sc_observability-1.4.0-cp310-abi3-win_amd64.whl` was built, but the verification command used Bash environment-variable syntax under PowerShell. Use an explicit Bash shell.
- crates.io returned HTTP429 for the tenth crate after nine successful publications. The idempotent retry, job106004523932, skipped those nine and published `sc-observability-py@1.4.0`. All ten crates are now published; preserve `already_published_channels=crates_io` in subsequent recovery. GitHub reran dependent build jobs as part of the job retry; no GitHub Release or post-release registry publication resulted.

## Immutable provenance

Preserve production tag `v1.4.0` at `2e27142afebf1d138d50d1468dbef182a187147e` and candidate tag `release-candidate-v1.4.0` at `dd3c368d2cda2217af9b50bf528d76d79bbceb7f`. Workflow-only recovery is built from newer authorized main under the shared retry policy. Never republish successful crates or move either tag.

GitHub Release, PyPI, and npm remain incomplete. A repaired main commit requires a new successful final preflight before retrying those channels.

## Shared fix adoption

Adopted immutable sc-publish `232c69597dd789c743fffc5fabab31228a450614` from [PR100](https://github.com/randlee/sc-publish/pull/100), a child of the existing source-installer pin. Regenerated installer-owned files from a Git archive of that commit using the pinned sc-compose1.5.0 renderer; repeat installer dry-run reports assets in sync. The changed assets select `RUSTUP_TOOLCHAIN` for maturin builds and specify Bash for platform verification. No crate or binding implementation, manifest/version, or package inventory changed.

Consumer validation:23 Python tests passed,2 toolchain-dependent tests skipped,10 subtests passed; `git diff --check` passed. Upstream reports216 tests passed,10 skipped,41 subtests, including focused real-rustup regression proof. Independent upstream review and hosted checks are tracked on PR100.
