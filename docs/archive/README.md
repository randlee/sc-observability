# Docs Archive

Historical sprint and execution docs that no longer belong at `docs/` root live
here so the active documentation set stays focused on current contracts and
current workstreams.

- `sprints/` contains completed or superseded sprint-planning material
