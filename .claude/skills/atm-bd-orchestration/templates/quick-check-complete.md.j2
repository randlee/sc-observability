---
name: quick-check-complete
version: 1.0.0
description: Quick-check report; the body of `atm task close <task-id> completed`. PASS closes the quick-check bead; FAIL leaves it open and the lead reopens the checked bead with these findings.
format: markdown
required_variables:
  - generated_at
  - task_id
  - checked_bead
  - sprint
  - branch
  - commit
  - verdict
  - findings_count
  - findings_md
  - lint_md
optional_variables:
  - notes_md
defaults:
  notes_md: ""
metadata:
  type: quick-check-complete
  tags:
    - stage:quick-check
    - orchestration:beads
  workflow:
    scope:
      kind: sprint
      variable: sprint
    state: quick-check-complete
    stage: quick-check
    transition: complete
---
{% if verdict not in ["PASS", "FAIL"] %}{{ verdict | verdict_must_be_PASS_or_FAIL }}ERROR: verdict must be PASS or FAIL, got {{ verdict }}
{% endif %}# Quick-Check {{ verdict }}

Generated: {{ generated_at }}
Bead: {{ task_id }} (sprint {{ sprint }})
Checked: {{ checked_bead }} on `{{ branch }}` at `{{ commit }}`
Verdict: **{{ verdict }}** ({{ findings_count }} findings)

## Machine Status (JSON)
```json
{
  "task": {{ task_id | tojson }},
  "checked_bead": {{ checked_bead | tojson }},
  "sprint": {{ sprint | tojson }},
  "branch": {{ branch | tojson }},
  "commit": {{ commit | tojson }},
  "verdict": {{ verdict | tojson }},
  "findings": {{ findings_count }}
}
```

## Findings
{{ findings_md }}

## Lint
{{ lint_md }}
{% if notes_md %}
## Notes
{{ notes_md }}
{% endif %}
